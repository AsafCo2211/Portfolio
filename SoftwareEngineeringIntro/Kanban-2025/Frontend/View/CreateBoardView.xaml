<Window x:Class="Frontend.View.CreateBoardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Board"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Prompt -->
        <TextBlock
      Grid.Row="0"
      Text="Enter new board name:"
      Margin="0,0,0,5"/>

        <!-- THIS needs an x:Name so code-behind sees it -->
        <TextBox
      x:Name="BoardNameTextBox"
      Grid.Row="1"
      Width="250"
      Margin="0,0,0,10"/>

        <StackPanel
      Grid.Row="2"
      Orientation="Horizontal"
      HorizontalAlignment="Right">
            <Button
        Content="OK"
        Width="75"
        Margin="0,0,5,0"
        IsDefault="True"
        Click="Ok_Button"/>
            <Button
        Content="Cancel"
        Width="75"
        IsCancel="True"
        Click="Cancel_Button"/>
        </StackPanel>
    </Grid>
</Window>
